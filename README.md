# AboutAnatoli

Мой персональный проект-портфолио с Django + REST API и React.

## Структура проекта

- **backend/** — Django + Django REST Framework
- **frontend/** — React фронтенд
- **docker-compose.yml** — для локального запуска всех сервисов

## Запуск локально

```bash
docker-compose up --build
